package virtual

// TODO: Write tests. For now we have some basic smoke tests via `make run-playground`. For now though
// examples/leaderregistry does a pretty good job of exercising a lot of the actual server logic since
// it spawns multiple real servers and makes them all communicate with each other over localhost.
